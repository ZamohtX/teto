version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: teto_user
      POSTGRES_PASSWORD: teto_password
      POSTGRES_DB: teto_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
